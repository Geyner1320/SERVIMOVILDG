<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dgteam.servimovildg">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_user"
        android:label="@string/NAME_ORGANITATION"
        android:roundIcon="@mipmap/icon_user"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Maps_users"
            android:label="@string/title_activity_maps_users"></activity>
        <activity
            android:name=".usuario.request_form"
            android:label="@string/Solicitar_Servicio"
            android:theme="@style/AppBaseTheme" />
        <activity
            android:name=".mecanico.details"
            android:label="@string/det" />
        <activity
            android:name=".mecanico.mechanic_list"
            android:label="@string/lista" />
        <activity
            android:name=".usuario.inbox_user"
            android:label="@string/chat"
            android:theme="@style/AppBaseTheme" />
        <activity
            android:name=".usuario.master_page_user"
            android:label="@string/title_activity_master_page"
            android:theme="@style/Theme.MyApplication.NoActionBar" />
        <activity android:name=".registro.SelectUserType" />
        <activity
            android:name=".mecanico.master_page_mechanic"
            android:label="@string/title_activity_master_page_mechanic"
            android:theme="@style/Theme.MyApplication.NoActionBar" />
        <activity android:name=".logueo.MainActivity" />
        <activity android:name=".registro.RegistroTallerActivity" />
        <activity android:name=".registro.RegistroCorreoPassActivity" />
        <activity
            android:name=".registro.RegistroActivity"
            tools:ignore="ExtraText" />
        <activity
            android:name=".SplashScreen"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>